apiVersion: apps/v1
kind: Deployment

metadata:
  name: demo12-raw

  labels:
    app.kubernetes.io/component: frontend
    app.kubernetes.io/name: demo12-raw

  annotations:
    argocd.argoproj.io/sync-wave: "20"

spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: demo12-raw

  template:
    metadata:
      labels:
        app.kubernetes.io/name: demo12-raw

    spec:
      containers:
      - name: demo-webserver
        image: quay.io/jberchez-redhat/demo-webserver:v12.0-todel
        ports:
          - name: http
            containerPort: 8080
