apiVersion: apps/v1
kind: Deployment

metadata:
  name: demo8-raw

  labels:
    app.kubernetes.io/component: frontend
    app.kubernetes.io/name: demo8-raw

  annotations:
    argocd.argoproj.io/sync-wave: "20"

spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: demo8-raw

  template:
    metadata:
      labels:
        app.kubernetes.io/name: demo8-raw

    spec:
      containers:
      - name: demo-webserver
        image: quay.io/jberchez-redhat/demo-webserver:v8.0-todel
        ports:
          - name: http
            containerPort: 8080
