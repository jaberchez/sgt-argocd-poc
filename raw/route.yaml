apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: demo6-raw

  labels:
    app.kubernetes.io/component: frontend
    app.kubernetes.io/name: demo6-raw

  annotations:
    argocd.argoproj.io/sync-wave: "22"

spec:
  port:
    targetPort: http

  tls:
    termination: edge

  to:
    kind: Service
    name: demo6-raw
    weight: 100

  wildcardPolicy: None
